import 'lithium-ui/checkbox';
import { LithiumCheckbox } from 'lithium-ui/checkbox';

describe('checkbox element', () => {
  it('should render', () => {
    expect(LithiumCheckbox).toBeTruthy();
  });
});
