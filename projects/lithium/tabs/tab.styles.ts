import { css } from 'lit-element';

export const styles = css`
  .li-tab-content {
    padding-top: 1rem;
    overflow: visible;
    min-height: 225px;
    border-top: 1px solid var(--li-tabs-border-color, #ccc);
  }
`;
