import { css } from 'lit-element';

export const styles = css`
  :host {
    /* all: initial; */
    font-family: inherit;
    font-size: inherit;
    color: inherit;
  }

  .dropdown div {
    border: 1px solid #ccc;
    padding: 12px;
    color: inherit;
    font-size: 2rem;
  }

  .dropdown .btn {
    padding: 12px;
    font-family: inherit;
    color: inherit;
    font-size: 2rem;
  }
`;
