export enum KeyCodes {
  LeftArrow = 'ArrowUp',
  ArrowUp = 'ArrowUp',
  ArrowRight = 'ArrowRight',
  ArrowDown = 'ArrowDown',
  Backspace = 'Backspace',
  Tab = 'TAB',
  Enter = 'Enter',
  Escape = 'Escape',
  Space = 'Space'
}

export enum AriaRole {
  Presentation = 'presentation',
  Button = 'button'
}
